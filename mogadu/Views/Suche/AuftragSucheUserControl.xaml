<UserControl x:Class="mogadu.Views.Suche.AuftragSucheUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:mogadu.Views.Suche"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid ItemsSource="{Binding AlleMitarbeiter}" IsReadOnly="True">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid ItemsSource="{Binding Teammitglieder}" IsReadOnly="True">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
